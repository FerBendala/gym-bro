// Componentes principales
export { ModernPage, ModernSection } from './components';
export { Layout as ModernLayout } from './layout';

// Hooks
export { useModernNavigation } from './hooks';

// Tipos
export type {
  ModernLayoutProps, ModernNavItem, ModernPageProps,
  ModernSectionProps,
  NavigationConfig, NavigationItem, NavigationType
} from './types';
